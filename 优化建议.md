# 企业访客预约系统 · 优化建议清单

> 基于对项目源码的实际审查，按 **优先级 × 维度** 整理待处理的优化点。
> 标注说明：🔴 高优先级｜🟡 中优先级｜🟢 低优先级（锦上添花）
> **v3.0：已完成项已删除，仅保留待处理。**

---

## 目录

- [一、架构优化](#一架构优化)
- [二、性能优化](#二性能优化)
- [三、数据库优化](#三数据库优化)
- [四、代码质量](#四代码质量)
- [五、功能完善](#五功能完善)
- [六、前端体验](#六前端体验)
- [七、运维部署](#七运维部署)
- [优化路线图](#优化路线图)

---

## 一、架构优化

---

### 🟢 1.2 DTO 层完善

**现状**：Controller 大量使用 `@RequestBody Map<String, String>` 接收参数，无类型安全。

**建议**：

```java
public class ApproveReq {
    @NotNull @Pattern(regexp = "approved|rejected")
    private String status;
    private String remark;
}
```

---

### 🟢 1.3 缓存层引入

**现状**：系统配置、节假日、角色权限等低频数据每次查库。

**建议**：引入 Spring Cache + Caffeine 或 Redis。

---

## 二、性能优化

### 🟡 2.1 分页保护

**现状**：`/appointment/host/pending`、`/admin/appointment/pending` 无分页，全量返回。

**建议**：统一加分页参数，设置 `size` 上限（如 max 100）。

---

### 🟢 2.2 二维码缓存

**现状**：每次 `GET /appointment/{id}/qrcode` 都重新用 ZXing 生成。

**建议**：生成后缓存 Base64 字符串，或前端缓存。

---

### 🟢 2.3 数据库连接池调优

**建议**：

```yaml
spring.datasource.hikari:
  maximum-pool-size: 20
  minimum-idle: 5
```

---

## 三、数据库优化

### 🟡 3.1 字段长度校验前移

**现状**：依赖 `DataIntegrityViolationException` 兜底「Data too long」。

**建议**：实体类加 `@Size` 校验。

---

### 🟢 3.2 审计字段完善

**建议**：`app_appointment` 加 `created_by` / `updated_by`，配合 MyBatis-Plus 自动填充。

---

### 🟢 3.3 剩余表软删除补充

**现状**：`app_greeting`、`app_visit_record`、`not_holiday` 未加 `deleted` 字段，删除为物理删除。影响较小，按需补充。

---

## 四、代码质量

### 🟢 4.1 单元测试补充

**现状**：`src/test` 下无业务测试。

**建议**：核心 Service（审批、核验、AI）补单元测试。

---

## 五、功能完善

### 🟡 5.1 真正对接微信登录

**现状**：`/auth/login` 直接委托 bypass，未调微信 `code2session`。

**建议**：对接 `jscode2session` 获取 openid，实现微信原生登录。

---

### ~~5.2 AI 异常访客风险预警（进阶任务）~~ ✅ 已完成

**已完成**：实现了基于 TF-IDF 向量化的轻量级 RAG 风险预警系统——历史被拒记录向量化存入内存知识库，新预约提交时余弦相似度检索 Top-5 相似记录，大模型评估风险等级（低/中/高）并给出预警理由，高风险自动通知管理员。

---

### 🟢 5.3 数据导出

**建议**：统计页支持导出 Excel（EasyExcel）。

---

## 六、前端体验

### 🟡 6.1 Token 无感刷新

**现状**：Token 24h 过期后直接 401 跳登录。

**建议**：静默刷新或滑动续期。

---

### 🟢 6.2 baseUrl 配置化

**现状**：`app.js` 硬编码 `http://localhost:8080`。

**建议**：按 envVersion 切换。

---

### 🟢 6.3 骨架屏与下拉刷新

**建议**：列表页加骨架屏 + `onPullDownRefresh`。

---

### 🟢 6.4 离线二维码展示

**建议**：首次生成后缓存本地。

---

## 七、运维部署

### 🟡 7.1 生产环境配置

**现状**：仅 `dev` 配置。

**建议**：加 `application-prod.yml`，敏感值走环境变量。

---

### 🟡 7.2 日志文件化

**现状**：日志输出控制台。

**建议**：`logback-spring.xml` 滚动文件 + 按天切割。

---

### 🟡 7.3 健康检查与监控

**建议**：Spring Boot Actuator + Prometheus + Grafana。

---

### 🟢 7.4 CI/CD 流水线

**建议**：GitHub Actions 自动构建部署。

---

### 🟢 7.5 容器化部署

**建议**：Dockerfile + docker-compose 一键启动。

---

## 优化路线图

| 阶段 | 任务 |
|------|------|
| **第二阶段：架构** | 2.1 分页保护 |
| **第三阶段：功能** | 5.1 微信登录、6.1 Token 无感刷新 |
| **第四阶段：运维** | 7.1 生产配置、7.2 日志文件、7.3 监控、7.4 CI/CD、7.5 容器化 |

---

> **已完成的优化**：密码 BCrypt 加密、配置环境变量化、CORS 收紧、接口数据级权限校验、异常脱敏、状态常量、SQL 聚合统计、趋势优化、逻辑删除+索引、事务边界+Service 下沉、二维码签名防伪、通知体系重构、部门管理修复、AI 异步化、日志风格统一、残留代码清理、预约时间冲突校验、预约定时提醒、AI 异常访客风险预警（RAG 向量检索 + 大模型评估）、辅助预约 visitorId=NULL 全链路 null-safety 加固、**DeepSeek 推理模型适配（Prompt 无 Few-Shot + max_tokens=4096(话术)/2048(风险) + lastIndexOf JSON 解析）**、**风险判定标准重构（ruleBased 永不判 high + 硬规则同记录双匹配 + AI→high 安全降级到 medium）**、**QR 按钮 Canvas+writeFile 双路径**、**再次预约功能删除**、**MyBatis SQL 调试日志关闭**。
> **v3.5**：DeepSeek 推理模型全适配 + 风险判定标准三级重构 + QR 保存双路径 + 再次预约删除 + 话术 Prompt 零示例。
