# 企业访客预约系统 · 新人接手学习手册

> 本手册从 **业务 / 技术 / 数据库 / 接口 / 部署 / 线上坑点** 六个维度全面拆解项目，并附核心业务执行流程，帮助新人快速上手。
> **v2.0 已同步安全加固、性能优化、密码加密、前端通知修复等改动。**

---

## 目录

- [一、业务维度](#一业务维度)
- [二、技术维度](#二技术维度)
- [三、数据库维度](#三数据库维度)
- [四、接口维度](#四接口维度)
- [五、部署维度](#五部署维度)
- [六、线上坑点](#六线上坑点)
- [附：核心业务执行流程](#附核心业务执行流程)

---

## 一、业务维度

### 1.1 系统定位

这是一套 **企业访客预约管理系统**，通过微信小程序让访客在线预约、被访人/管理员审批、门岗扫码核验，形成「预约 → 审批 → 核验 → 接待」的完整闭环。核心亮点是 **AI 接待话术自动生成**（课设必选任务）和 **AI 异常访客风险预警**（课设加分任务），详见 [`重要功能.md`](重要功能.md)。

### 1.2 四大角色与职责

| 角色 | role 值 | 核心职责 | 关键页面目录 |
|------|---------|---------|-------------|
| **管理员** | `admin` | 人员/部门/系统配置管理、预约审批、通知发布、数据统计 | `pages/admin/*`（9 个子页面） |
| **被访人** | `host` | 审批来访自己的预约、查看被访记录与统计、辅助预约 | `pages/host/*`（3 个子页面） |
| **访客** | `visitor` | 提交/撤销/再次预约、查看预约记录与二维码、查看通知 | `pages/visitor/*`（4 个子页面） |
| **门岗** | `guard` | 扫码核验（或按姓名+手机号核验）、确认放行 | `pages/guard/scan` |

### 1.3 预约状态机（核心）

```
                   访客提交
                     │
                     ▼
                 ┌────────┐
                 │pending │ (待审核)
                 └───┬────┘
           ┌─────────┼─────────┐
      审批通过      审批拒绝     访客撤销
           │         │         │
           ▼         ▼         ▼
      ┌────────┐ ┌────────┐ ┌──────────┐
      │approved│ │rejected│ │cancelled │
      └───┬────┘ └────────┘ └─────┬────┘
          │                         │
     门岗核验+放行            访客可"恢复"
          │                         │
          ▼                         └──→ 回到 pending
      ┌──────────┐
      │confirmed │ (已核验，终态)
      └──────────┘
```

**5 种状态**：`pending` → `approved` / `rejected` / `cancelled` → `confirmed`

- **只有 `pending` 状态可审批**（`AppointmentService.approve` 有校验）
- **只有 `pending` 状态可撤销**（`AppointmentService.cancel` 有校验 + **归属校验**）
- **`cancelled` 可恢复为 `pending`**（`AppointmentController.restore`，**校验归属**）
- **`approved` 或 `confirmed` 都可通过门岗核验**（`GuardController.verify`）
- **已删除「再次预约」功能**（rebook 接口及相关代码已清理）

> 状态值统一定义在 `common/constant/AppointmentStatus.java` 常量类中，消除魔法值。

### 1.4 业务模块全景

```
企业访客小程序
├── 管理员模块
│   ├── 系统设置（公司名/Logo/电话/地址/工作时间/访客须知）
│   ├── 角色权限（super/admin/host/visitor/guard 权限标识管理）
│   ├── 管理员管理（增删改查）
│   ├── 员工管理（条件查询 + 增删改查 + 批量删除/启用/停用）
│   ├── 部门管理（树形结构，parent_id 关联）
│   ├── 节假日设置（法定/公司）
│   ├── 访客记录统计（时间区间/来访单位/部门维度）
│   ├── 访客申请审核（全局待审核列表）
│   └── 通知管理（按角色发布通知公告 + 我的通知含风险预警）
├── 被访人模块
│   ├── 被访记录（含时间区间统计 + 来访单位统计）
│   ├── 访客申请审核（仅审批 hostId=自己 的预约）
│   ├── 辅助预约（代访客填单，visitorId 留空，全链路 null-safe）
│   └── 个人资料
├── 访客模块
│   ├── 预约申请（先通过姓名+手机号查找被访人）
│   ├── 预约记录（二维码 / 撤销 / 恢复）
│   ├── 查看通知公告
│   ├── 消息中心（通知中心：审批结果/欢迎莅临/公告）
│   └── 个人资料
├── 门岗模块
│   ├── 扫码核验（wx.scanCode 扫二维码）
│   ├── 手动核验（按姓名+手机号查找，备用方案）
│   └── 确认放行（更新状态为 confirmed + 记录核验日志）
└── AI 模块
    ├── 接待话术智能生成（必选任务：审批通过自动触发 + 手动重新生成）
    └── 异常访客风险预警（进阶任务：RAG 向量检索 + 大模型评估 + 高风险通知管理员）
```

---

## 二、技术维度

### 2.1 技术栈总览

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **后端框架** | Spring Boot | 3.2.4 | Web + AOP + Validation + **@Async 异步 + @Scheduled 定时** |
| **ORM** | MyBatis-Plus | 3.5.7 | 持久层，继承 `ServiceImpl`，支持 `@TableLogic` 逻辑删除 |
| **数据库** | MySQL | 8.0+ | 关系型存储 |
| **鉴权** | JWT (jjwt) | 0.12.5 | 无状态 Token 认证 |
| **密码加密** | Spring Security Crypto | - | **BCrypt 密码加密与校验** |
| **二维码** | ZXing | 3.5.3 | 生成预约二维码（Base64 返回） |
| **AI 调用** | Spring WebFlux | - | `WebClient` 异步调 DeepSeek API |
| **接口文档** | SpringDoc OpenAPI | 2.5.0 | Swagger UI |
| **前端** | 微信小程序原生 | - | WXML/WXSS/JS |
| **Java** | JDK | 17 | 运行环境 |

### 2.2 后端分层架构

```
Controller（13 个）      ← 接收请求、参数校验、组装响应
    │
Service（10+ 个）        ← 业务逻辑，继承 MyBatis-Plus ServiceImpl
    │
Mapper（10 个）          ← MyBatis-Plus BaseMapper + @Select 注解查询
    │
Entity（10 个）          ← 数据库实体，Lombok @Data + @TableLogic 逻辑删除
```

**横切关注点：**

| 组件 | 文件 | 作用 |
|------|------|------|
| **鉴权拦截器** | `auth/AuthInterceptor.java` | 解析 JWT → 注入 userId/role → **基于角色的路由白名单校验** |
| **JWT 工具** | `auth/JwtUtil.java` | Token 生成/解析，secret 通过环境变量注入，有效期 24h |
| **密码编码器** | `config/PasswordConfig.java` | **BCryptPasswordEncoder Bean**，登录校验 + 注册加密 |
| **全局异常** | `common/exception/GlobalExceptionHandler.java` | 捕获异常，**未知异常脱敏返回通用提示** |
| **统一响应** | `common/Result.java` | `{ code, msg, data }` 三段式 |
| **状态常量** | `common/constant/AppointmentStatus.java` | **预约状态常量类**，消除魔法值 |
| **CORS 配置** | `config/WebMvcConfig.java` | 跨域来源**通过 `${cors.allowed-origins}` 配置注入** |
| **AI 客户端** | `ai/DeepSeekClient.java` | 调用大模型生成话术，**失败自动回退模板** |
| **异步线程池** | `config/AsyncConfig.java` | @EnableAsync + ThreadPoolTaskExecutor（核心2线程），AI 话术异步生成 |
| **定时提醒** | `task/ReminderScheduler.java` | @Scheduled 每5分钟检查30分钟内开始的预约，发提醒通知 |

### 2.3 鉴权机制详解（重点）

**认证流程：**

1. 登录 `POST /auth/login/bypass` → **BCrypt 校验密码** → `JwtUtil.generate(userId, role)` 生成 Token
2. 前端 `request.js` 自动在 Header 加 `Authorization: Bearer <token>`
3. `AuthInterceptor.preHandle` 拦截所有请求（排除 `/auth/**`、`/swagger-ui/**` 等）
4. 解析 Token → 取出 `userId` 和 `role` → 存入 `request.setAttribute`
5. **按角色路由白名单匹配**当前 URI，不匹配返回 403

**角色路由白名单**（`AuthInterceptor.ROLE_ROUTES`）：

| 角色 | 可访问路由（`*` 单段通配，`**` 多段通配） |
|------|------------------------------------------|
| admin | `/admin/**`、`/appointment/host/**`、`/appointment/helper`、`/appointment/*/approve`、`/notification`、`/user-notification/**`、`/ai/**`、`/user/profile` |
| host | `/appointment/host/**`、`/appointment/helper`、`/appointment/*/approve`、`/notification`、`/user-notification/**`、`/ai/**`、`/user/profile` |
| visitor | `/appointment`、`/appointment/my`、`/appointment/*/cancel`、`/appointment/*/restore`、`/appointment/host/lookup`、`/appointment/*/qrcode`、`/ai/appointment/*/greeting`、`/notification`、`/user-notification/**`、`/user/profile` |
| guard | `/guard/**`、`/notification`、`/user-notification/**`、`/user/profile` |

> **双层权限校验**：路由白名单做角色级拦截，Controller 内部做**数据级校验**——`cancel`/`restore`/`rebook`/`qrcode` 均校验资源归属（visitorId == 当前用户），`approve` 校验 host 只能审批 hostId == 自己的预约。

### 2.4 前端架构

```
miniprogram/
├── app.js          ← 全局 App 实例：token 管理
├── app.json        ← 页面路由注册（21 个页面）
├── utils/
│   ├── request.js  ← 统一网络请求封装（自动带 Token，401 自动跳登录）
│   ├── api.js      ← 所有接口路径常量定义
│   └── data-service.js
└── pages/          ← 按角色分目录
```

**前端关键机制：**

- **Token 存储**：`wx.setStorageSync('token', ...)`，`request.js` 自动读取并注入 Header
- **401 处理**：响应 `code === 401` 时清除 Token + `reLaunch` 跳登录页
- **通知机制**：管理员发布公告时自动分发到 `sys_user_notification` 表；普通用户通过通知中心查看；管理员通过「通知管理→我的通知」标签页查看（含风险预警）；点击即标记已读

### 2.5 AI 话术生成机制（重点）

**触发时机：**

1. **自动触发**：预约审批通过时，`AppointmentController.approve` 调用 `appointmentService.generateGreetingIfNeeded`（**事务外执行**，失败不影响审批）
2. **手动触发**：`POST /ai/greeting/{id}`（有缓存直接返回）/ `POST /ai/greeting/{id}/regenerate`（清除缓存后强制重生成）

**模型**：`deepseek-v4-flash-free`（推理模型，会先生成 reasoning_content 再输出 content）

**调用链：**

```
AppointmentService.generateGreetingIfNeeded(appointmentId)
    │
    ├─ 检查 app_greeting 是否已存在 → 已存在则跳过
    │
    └─ DeepSeekClient.generateGreeting(visitorName, company, purpose, hostName)
         │
         ├─ Redis/内存缓存命中(12h) → source="cache"
         ├─ API Key 为空？→ 返回模板话术 (source="mock")
         │
         ├─ 构造零示例 Prompt（推理模型适配，无 Few-Shot）
         ├─ WebClient.post() 调 DeepSeek API
         │     model: deepseek-v4-flash-free
         │     temperature: 0.7, max_tokens: 4096, timeout: 30s
         │
         ├─ parseRobustJson(content): lastIndexOf("{\"greeting\"")
         │     （推理模型先输出分析再输出JSON，取最后一个JSON对象）
         │     解析失败 → 回退模板
         │
         └─ 返回 Map { greeting, seatSuggestion, notes, source }
              source="ai" 表示真实 AI，source="mock" 表示模板兜底
```

**容错策略**：API Key 未配置 / 超时 / content 为空（推理 token 不足）/ JSON 解析失败 → 全部回退模板话术。

> ⚠️ **推理模型坑点**：`max_tokens` 必须 ≥ 4096，否则推理阶段吃光 token，content 为空。Prompt 不能用 Few-Shot（模型会把示例当分析题）。JSON 解析必须用 `lastIndexOf`（推理文本在前）。

---

## 三、数据库维度

### 3.1 数据库概览

- **数据库名**：`visitor_system`
- **字符集**：`utf8mb4 / utf8mb4_unicode_ci`
- **存储引擎**：InnoDB
- **共 10 张表**，分三类前缀：

| 前缀 | 含义 | 表 |
|------|------|-----|
| `sys_` | 系统管理类 | `sys_department`、`sys_user`、`sys_role_permission`、`sys_setting`、`sys_user_notification` |
| `app_` | 业务应用类 | `app_appointment`、`app_greeting`、`app_visit_record`、`app_risk_assessment` |
| `not_` | 通知公告类 | `not_notification`、`not_holiday` |

### 3.2 核心表结构详解

#### `sys_user`（用户表，四角色共用）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | 自增主键 |
| username | VARCHAR(50) UNIQUE | 登录名 |
| password | VARCHAR(100) | 密码（**✅ BCrypt 加密存储**） |
| name | VARCHAR(50) | 姓名 |
| role | ENUM('admin','host','visitor','guard') | 角色 |
| phone | VARCHAR(20) | 手机号 |
| department_id | INT | 部门 ID（访客为 NULL） |
| position | VARCHAR(50) | 职位 |
| company | VARCHAR(100) | 所属公司（访客使用） |
| status | TINYINT | 1 启用 0 停用 |
| deleted | TINYINT | **✅ 逻辑删除 0未删 1已删** |

#### `app_appointment`（预约表，核心业务表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | 自增主键 |
| visitor_id | INT NULL | 访客用户 ID（**辅助预约/游客可为 NULL**） |
| visitor_name | VARCHAR(50) | 访客姓名 |
| visitor_phone | VARCHAR(20) | 访客手机号 |
| company | VARCHAR(100) | 来访单位 |
| purpose | VARCHAR(200) | 来访事由 |
| car_plate | VARCHAR(20) | 车牌号 |
| visitor_count | INT | 来访人数 |
| host_id | INT | 被访人用户 ID |
| host_name | VARCHAR(50) | 被访人姓名 |
| start_time | DATETIME | 预计到访时间 |
| end_time | DATETIME | 预计离开时间 |
| status | ENUM | `pending/approved/rejected/cancelled/confirmed` |
| remark | VARCHAR(500) | 备注（审批时存拒绝原因） |
| deleted | TINYINT | **✅ 逻辑删除** |

**索引**：`idx_visitor`、`idx_host`、`idx_status`、`idx_time`、**`idx_visitor_status`（组合）**、**`idx_host_time`（组合）**

#### `app_greeting`（AI 话术记录表）

| 字段 | 说明 |
|------|------|
| appointment_id | 预约 ID |
| greeting_text | 迎接语 |
| seat_suggestion | 座位建议（字段存在但当前未填充） |
| notes | 注意事项 |
| status | `pending/completed/failed` |

#### `app_risk_assessment`（AI 风险预警记录表）

| 字段 | 说明 |
|------|------|
| appointment_id | 预约 ID |
| risk_level | ENUM('low','medium','high') 风险等级 |
| risk_score | 风险分数 0-100 |
| reason | 预警理由 |
| similar_cases | 相似历史案例（JSON 数组） |

#### `sys_user_notification`（用户通知表）

| 字段 | 说明 |
|------|------|
| user_id | 接收用户 ID |
| type | ENUM('approved','rejected','greeting','announcement','risk_warning') |
| title | 通知标题 |
| content | 通知内容（**greeting 类型存 JSON**） |
| appointment_id | 关联预约 ID（**公告类可为 NULL**） |
| is_read | 0 未读 1 已读 |

> `sys_department`、`not_notification` 也已加 `deleted` 逻辑删除字段。

### 3.3 表关系图

```
sys_department (id) ◄──── department_id ──── sys_user (id)
                                                    │
                                           host_id / visitor_id
                                                    │
                                                    ▼
                                          app_appointment (id)
                                           │        │        │
                            appointment_id  │        │        │  appointment_id
                                  ┌─────────┘        │        └──────────┐
                                  ▼                 │                   ▼
                           app_greeting             │          app_visit_record
                                  │            appointment_id        (guard_id → sys_user)
                                  │                 │
                                  │                 ▼
                                  │       app_risk_assessment
                                  │       (AI 风险预警：RAG 检索 + 大模型评估)
                                  │
                                  └──→ RiskVectorStore（内存 TF-IDF 向量库）
                                       历史被拒记录向量化 → 新预约余弦相似度检索

sys_user (id) ──── user_id ──→ sys_user_notification (appointment_id → app_appointment)

not_notification (target_role) ──按角色匹配──→ 所有用户（不入库，实时查）

sys_role_permission (role_key) ──权限标识──→ 角色权限配置
sys_setting (config_key/value) ──键值对──→ 系统配置
not_holiday (date) ──节假日──→ 日历管理
```

### 3.4 初始化数据要点

`insert.sql` 预置了：

- **7 个部门**（技术部/市场部/人事部/财务部/管理部 + 前端组/后端组）
- **10 个用户**（2 管理员 + 4 被访人 + 3 访客 + 1 门岗），**密码全部 BCrypt 加密**
- **8 条预约**（含已审批/待审批/已拒绝/游客预约）
- **2 条 AI 话术**、2 条核验记录、3 条通知公告、8 个节假日、角色权限配置、6 项系统配置

**预置账号**（密码已 BCrypt 加密，登录时自动校验）：

| 账号 | 密码 | 角色 |
|------|------|------|
| admin | admin123 | 管理员 |
| 其余用户 | 123456 | 被访人/访客/门岗 |

### 3.5 SQL 脚本执行顺序

```bash
# 1. 建库建表（含 deleted 字段 + 组合索引，appointment_id 已允许 NULL）
mysql -u root -p < forSql/init.sql

# 2. 填充初始数据（密码已 BCrypt 加密）
mysql -u root -p < forSql/insert.sql
```

> **仅需 2 个脚本**，alter 修补脚本已合并入 init.sql。

---

## 四、接口维度

### 4.1 通用约定

| 项目 | 值 |
|------|-----|
| **Base URL** | `http://localhost:8080`（`app.js` 中 baseUrl 不带 `/api` 前缀） |
| **认证头** | `Authorization: Bearer <token>` |
| **响应格式** | `{ "code": 200, "msg": "success", "data": {...} }` |
| **错误码** | 200 成功 / 400 参数错误 / 401 未登录 / 403 无权限 / 500 系统错误 |
| **文档地址** | `http://localhost:8080/swagger-ui.html` |

### 4.2 接口分类速查

#### 认证模块（`/auth/**`，免鉴权）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/login` | 微信登录（**当前直接委托 bypass，未真正对接 wx.login**） |
| POST | `/auth/login/bypass` | 账号密码登录（**BCrypt 校验**） |
| POST | `/auth/register` | 访客注册（**密码 BCrypt 加密存储**） |

#### 预约模块（`/appointment/**`）

| 方法 | 路径 | 角色 | 说明 |
|------|------|------|------|
| POST | `/appointment` | visitor | 提交预约 |
| GET | `/appointment/my` | visitor | 我的预约记录（分页+状态过滤） |
| PUT | `/appointment/{id}/cancel` | visitor | 撤销预约（**校验归属**） |
| PUT | `/appointment/{id}/restore` | visitor | 恢复已取消预约（**校验归属**） |
| POST | `/appointment/host/lookup` | visitor | 按姓名+手机号查找被访人 |
| GET | `/appointment/{id}/qrcode` | visitor | 生成预约二维码（**校验归属**） |
| GET | `/appointment/host` | host | 被访记录（分页+时间区间） |
| GET | `/appointment/host/stats` | host | 被访统计（**SQL 聚合**） |
| GET | `/appointment/host/pending` | host | 待审批列表 |
| PUT | `/appointment/{id}/approve` | host/admin | **审批预约（事务内审批+发通知，事务外生AI话术）** |
| POST | `/appointment/helper` | host/admin | 辅助预约（代填） |

#### 管理员模块（`/admin/**`）

| 路径前缀 | 说明 |
|---------|------|
| `/admin/employee` | 员工 CRUD + 批量操作 |
| `/admin/department` | 部门 CRUD（树形） |
| `/admin/admin` | 管理员 CRUD |
| `/admin/role/{id}` | 角色权限更新 |
| `/admin/settings` | 系统配置 GET/PUT |
| `/admin/holiday` | 节假日 CRUD |
| `/admin/appointment` | 全部预约/待审批 |
| `/admin/stats/*` | 数据统计（**SQL 聚合查询**） |
| `/admin/notification` | 通知管理 CRUD |

#### 门岗模块（`/guard/**`）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/guard/verify` | 扫码核验（qrCode 格式：`appointment_{id}`） |
| POST | `/guard/verify-by-name` | 手动核验（姓名+手机号） |
| PUT | `/guard/confirm/{id}` | 确认放行（→confirmed + 记录核验日志） |

#### AI 模块（`/ai/**`）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/ai/greeting/{id}` | 生成/获取话术（有缓存返回缓存） |
| POST | `/ai/greeting/{id}/regenerate` | 强制重新生成 |
| GET | `/ai/appointment/{id}/greeting` | 查看已生成话术 |

#### AI 风险预警模块（`/admin/risk/**`，管理员专用）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/admin/risk/appointment/{id}` | 查看某预约的风险评估结果 |
| GET | `/admin/risk/high` | 高风险预约列表 |
| GET | `/admin/risk/all` | 全部风险评估记录 |
| POST | `/admin/risk/appointment/{id}/reassess` | 手动重新评估 |
| GET | `/admin/risk/store/status` | 向量库状态（被拒记录数） |
| POST | `/admin/risk/store/rebuild` | 手动重建向量库 |

#### 通知模块（`/notification`、`/user-notification/**`）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/notification` | 公共通知列表（按角色过滤） |
| GET | `/user-notification/popup` | 未读通知（前端已不再调用） |
| GET | `/user-notification/list` | 通知中心全部通知 |
| PUT | `/user-notification/{id}/read` | 标记已读 |
| PUT | `/user-notification/read-all` | 全部已读 |

### 4.3 关键接口深度说明

**审批接口 `PUT /appointment/{id}/approve`** 执行流程（已优化）：

1. **权限校验**：host 角色只能审批 `hostId == 自己` 的预约（admin 不受限）
2. **事务内操作**（`AppointmentService.approve`，`@Transactional`）：
   - 状态更新：`pending` → `approved`/`rejected`，存 remark
   - 给访客发通知（approved → type=approved，rejected → type=rejected）
3. **事务外操作**（`AppointmentService.generateGreetingIfNeeded`）：
   - 检查已有话术 → 已有则跳过
   - 调 AI 生成话术 → 存 `app_greeting` 表
   - **失败不影响审批结果**（try-catch 兜底）

**AI 风险预警流程**（`AppointmentService.assessRiskIfNeeded`，`@Async` 异步，详见 [`重要功能.md`](重要功能.md)）：

1. **触发时机**：访客提交预约（`POST /appointment`）、辅助预约时自动触发
2. **RAG 向量检索**（`RiskVectorStore.search` v2.0）：
   - 知识库 = 所有 `status=rejected` 的历史预约
   - 向量化 = TF-IDF（字符二元组分词 + L2 归一化）
   - 检索 = 余弦相似度 Top-5 + 手机号/姓名/公司加权融合（∈[0,1]）+ 动态阈值（下限0.10）
3. **硬规则优先**（`AppointmentService.checkHardRules`，零 token）：
   - 同一被拒记录内：手机号+姓名双匹配 → high(95)
   - 同一被拒记录内：手机号+单位双匹配 → high(85)
   - 不满足 → 走 AI 评估
4. **大模型评估**（`DeepSeekClient.assessRisk`，max_tokens=2048，简化 Prompt）：
   - 返回 `riskLevel`、`riskScore`、`reason`
   - API 不可用 → 规则兜底（永远不判 high，最高 medium）
5. **安全锁**：非硬规则源的 high 强制降为 medium（仅硬规则可判 high）
6. **高风险通知**：`riskLevel=high` 时通知所有管理员
7. **拒绝入库**：拒绝预约后加入向量库 + 清除风险缓存

> ⚠️ **关键设计**：`ruleBased` 永不判 high（2-gram 假阳性多）。真正的 high 仅来自同记录内手机号+姓名/单位双匹配（硬规则）。

**扫码核验 `POST /guard/verify`** 执行了：

1. 解析二维码内容 `appointment_{id}` → 提取预约 ID
2. 查预约，校验状态为 `approved` 或 `confirmed`（`AppointmentStatus.VERIFIABLE`）
3. 核验成功 → 给访客发「欢迎莅临」通知（type=greeting，content 存话术 JSON）
4. **通知防重复**：`UserNotificationService.createNotification` 内部按 `userId+appointmentId+type` 去重

---

## 五、部署维度

### 5.1 环境要求

| 环境 | 要求 |
|------|------|
| JDK | **17+**（Spring Boot 3 最低要求） |
| Maven | 3.6+ |
| MySQL | 8.0+ |
| 微信开发者工具 | 最新稳定版 |

### 5.2 本地开发部署步骤

```
① 初始化数据库（仅需 2 个脚本）
   mysql -u root -p < forSql/init.sql
   mysql -u root -p < forSql/insert.sql

② 配置后端（通过环境变量或使用默认值）
   设置环境变量（可选，不设则用默认值）：
   DB_PASSWORD=你的数据库密码
   JWT_SECRET=你的JWT密钥
   DEEPSEEK_API_KEY=你的AI密钥

   或直接编辑 backend/src/main/resources/application-dev.yml

③ 启动后端
   cd backend
   mvn spring-boot:run
   → 访问 http://localhost:8080/swagger-ui.html 验证

④ 导入前端
   微信开发者工具 → 导入 demofront 目录
   → 详情 → 本地设置 → 勾选「不校验合法域名...」
   → 确认 app.js 中 baseUrl = 'http://localhost:8080'

⑤ 登录测试
   使用预置账号：admin/admin123、zhangsan/123456 等
```

### 5.3 配置文件说明

**`application.yml`**（主配置，**敏感值已环境变量化**）：

```yaml
server.port: 8080
jwt.secret: ${JWT_SECRET:默认开发密钥}        # ✅ 环境变量注入
jwt.expiration: 86400000                      # Token 有效期 24h
deepseek.api-key: ${DEEPSEEK_API_KEY:}        # ✅ 环境变量注入
deepseek.api-url: ${DEEPSEEK_API_URL:...}
deepseek.model: ${DEEPSEEK_MODEL:...}
cors.allowed-origins: ${CORS_ORIGINS:http://localhost:*}  # ✅ CORS 配置化
mybatis-plus:
  configuration.map-underscore-to-camel-case: true
  global-config.db-config:
    logic-delete-field: deleted                # ✅ 逻辑删除生效（表已有 deleted 字段）
```

**`application-dev.yml`**（开发环境，**密码已环境变量化**）：

```yaml
spring.datasource:
  url: ${DB_URL:jdbc:mysql://localhost:3306/visitor_system?...}
  username: ${DB_USERNAME:root}
  password: ${DB_PASSWORD:}                    # ✅ 环境变量注入
logging.level.com.visitor: debug
```

### 5.4 演示/线上部署方案

| 方案 | 适用场景 | 操作 |
|------|---------|------|
| **内网穿透** | 课设演示 | natapp / ngrok 将 `localhost:8080` 映射为 HTTPS 公网地址 |
| **云托管** | 长期运行 | Railway / Zeabur 部署 Spring Boot + 云数据库 |
| **微信合法域名** | 正式上线 | 小程序后台配置 request 合法域名（需 HTTPS） |

### 5.5 构建打包

```bash
cd backend
mvn clean package -DskipTests
# 产物：target/visitor-system-1.0.0.jar
java -jar target/visitor-system-1.0.0.jar
```

---

## 六、线上坑点

> 以下分为 **已修复** ✅ 和 **待处理** ⚠️ 两类。

### 6.1 已修复问题 ✅

| # | 问题 | 修复方案 |
|---|------|---------|
| 1 | ~~密码明文存储~~ | ✅ **BCrypt 加密存储**，登录用 `passwordEncoder.matches` 校验，注册用 `encode` 加密 |
| 2 | ~~JWT 密钥硬编码~~ | ✅ **改为 `${JWT_SECRET:默认值}`** 环境变量注入 |
| 3 | ~~DeepSeek API Key 提交仓库~~ | ✅ **改为 `${DEEPSEEK_API_KEY:}`** 环境变量注入 |
| 4 | ~~数据库密码提交仓库~~ | ✅ **改为 `${DB_PASSWORD:}`** 环境变量注入 |
| 5 | ~~CORS 全放开~~ | ✅ **改为 `${cors.allowed-origins}`** 配置注入 |
| 6 | ~~异常返回原始错误信息~~ | ✅ **未知异常返回「系统繁忙」**，详细日志记文件 |
| 7 | ~~LoginResp departmentId 永远 null~~ | ✅ **修复为返回 `user.getDepartmentId()`** |
| 8 | ~~deptStats 永远为空~~ | ✅ **关联 `sys_department` 表 SQL 聚合统计** |
| 9 | ~~逻辑删除配置无效~~ | ✅ **业务表加 `deleted` 字段 + 实体类加 `@TableLogic`** |
| 10 | ~~统计接口全表扫描+内存聚合~~ | ✅ **改为 SQL `GROUP BY` 聚合查询** |
| 11 | ~~趋势统计循环 N 次查库~~ | ✅ **改为一条 SQL `GROUP BY DATE` + Java 补零** |
| 12 | ~~审批+发通知非原子~~ | ✅ **审批+发通知下沉 Service 同事务，AI 话术事务外** |
| 13 | ~~接口缺少数据级权限校验~~ | ✅ **cancel/restore/rebook/qrcode 均校验资源归属** |
| 14 | ~~状态字符串魔法值散落~~ | ✅ **统一到 `AppointmentStatus` 常量类** |
| 15 | ~~弹窗通知强制跳转~~ | ✅ **通知体系重构，全部走通知中心** |
| 16 | ~~SQL 需 3 个脚本~~ | ✅ **合并为 2 个**（init.sql + insert.sql），alter 逻辑并入 init.sql |

### 6.2 待处理问题 ⚠️

| # | 问题 | 位置 | 建议 |
|---|------|------|------|
| 1 | **`/auth/login` 微信登录未真正实现** | `LoginController.login()` 直接委托 bypass | 对接微信 `jscode2session` 获取 openid |
| 2 | ~~AI 调用同步~~ ✅ | 已改为 `@Async("aiExecutor")`，审批接口立即返回 | 完成 |
| 3 | ~~二维码可伪造~~ ✅ | 已加 HMAC-SHA256 签名 + 2h 有效期 | 完成 |
| 4 | ~~公告 ID 偏移冲突~~ ✅ | 已改为仅查 `sys_user_notification` 单表 | 完成 |
| 5 | **无分页保护** | `/appointment/host/pending`、`/admin/appointment/pending` 无分页 | 加默认分页 + size 上限 |
| 6 | **JWT 无刷新机制** | Token 24h 过期后直接 401 | 加 refresh token 或滑动续期 |
| 7 | **baseUrl 硬编码 localhost** | `app.js:5` | 改为按 envVersion 切换 |
| 8 | ~~辅助预约 visitorId=NULL~~ ✅ | 已全面加固：通知/提醒/权限校验/二维码 全链路 null-safe | 完成 |
| 9 | **接口限流缺失** | AI 接口可被刷调用 | 引入 Redis + Bucket4j 限流 |
| 10 | **无环境隔离** | 仅 dev 配置，无 prod | 加 `application-prod.yml` |

---

## 附：核心业务执行流程

### 流程一：访客预约 → 审批 → 核验（完整主链路）

```
┌─────────────────────────────────────────────────────────────────────┐
│  访客端                    后端                       被访人/管理员    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 登录（BCrypt 校验）                                              │
│  POST /auth/login/bypass ──→ passwordEncoder.matches() ──→ Token   │
│                                                                     │
│  2. 查找被访人                                                      │
│  POST /appointment/host/lookup                                      │
│    {name, phone} ──→ 查 sys_user(role=host) ──→ {id, name}          │
│                                                                     │
│  3. 提交预约（含时间冲突校验）                                       │
│  POST /appointment ──→ checkTimeConflict(hostId, start, end)        │
│                    ──→ save(status=PENDING) ──→ {appointmentId}     │
│                                                                     │
│                              4. 被访人查看待审批                     │
│                              GET /appointment/host/pending           │
│                                  ←── 返回 pending 列表               │
│                                                                     │
│                              5. 审批预约                             │
│                              PUT /appointment/{id}/approve           │
│                                {status: "approved"}                 │
│                                    │                                │
│                              ┌─ 事务内 ─────────────────────────┐  │
│                              │ ├─ 权限校验(hostId==自己)         │  │
│                              │ ├─ 更新 status=APPROVED           │  │
│                              │ └─ 发通知给访客(type=approved)    │  │
│                              └────────────────────────────────────┘  │
│                              ┌─ 事务外 ─────────────────────────┐  │
│                              │ generateGreetingIfNeeded()        │  │
│                              │ ├─ 检查已有话术 → 有则跳过        │  │
│                              │ └─ 调 AI → 存 app_greeting 表     │  │
│                              │    (失败不影响审批，try-catch)    │  │
│                              └────────────────────────────────────┘  │
│                                                                     │
│  6. 访客查看预约记录（含二维码，校验归属）                            │
│  GET /appointment/my ──→ 返回记录列表                                │
│  GET /appointment/{id}/qrcode ──→ ZXing 生成 Base64 二维码           │
│                                                                     │
│                              7. 门岗扫码核验                         │
│                              POST /guard/verify                      │
│                                {qrCode: "appointment_5"}             │
│                                    │                                │
│                                    ├─ 解析 id=5                      │
│                                    ├─ 校验 status∈VERIFIABLE        │
│                                    ├─ 发"欢迎莅临"通知给访客          │
│                                    │   (type=greeting, 存话术JSON)   │
│                                    └─ 返回访客信息                   │
│                                                                     │
│                              8. 门岗确认放行                         │
│                              PUT /guard/confirm/{id}                 │
│                                    ├─ 更新 status=CONFIRMED          │
│                                    └─ 记录 app_visit_record          │
│                                                                     │
│  9. 访客进入通知中心查看通知                                      │
│  app.js 不再自动弹窗，用户主动进入通知中心                           │
│    → 未读 tab 显示所有未读通知                                       │
│    → 点击通知 → 标记已读 → 弹出详情                                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 流程二：AI 话术生成时序

```
被访人审批通过
    │
    ▼
AppointmentController.approve()
    │
    ├─ 事务内: appointmentService.approve(id, status, remark, operatorId, role)
    │      → 校验 pending 状态 + 权限(hostId==自己)
    │      → 更新 status
    │      → 发通知给访客(type=approved/rejected)
    │
    └─ 事务外: appointmentService.generateGreetingIfNeeded(id)
           │
           ├─ 检查 app_greeting 是否已存在 → 已存在则跳过
           │
           └─ deepSeekClient.generateGreeting()
                 │
                 ├─ API Key 为空? → mockGreeting() (source="mock")
                 │
                 ├─ 构造 Prompt (访客名+单位+事由+被访人)
                 │
                 ├─ WebClient.post(deepseek API)
                 │
                 ├─ 解析 JSON {greeting, notes}
                 │     失败 → mockGreeting()
                 │
                 └─ 存入 app_greeting 表 (status=completed)
```

### 流程三：通知体系数据流

```
                          ┌─────────────────┐
                          │  not_notification│  公告表（管理员发布）
                          │  target_role     │
                          └────────┬────────┘
                                   │ 实时查询，不入库
                                   ▼
┌─────────────────┐         ┌──────────────────────┐
│ 审批/核验事件    │────────→│ sys_user_notification │  个人通知表
│ approve/reject/ │         │ user_id, type,        │
│ greeting        │         │ appointment_id, is_read│
└─────────────────┘         └──────────┬───────────┘
                                       │
                              前端 GET /user-notification/list
                                       │
                          ┌────────────┴────────────┐
                          │ 通知中心：未读 / 已读 / 全部│
                          │ 点击未读 → 标记已读 → 弹详情│
```

### 流程四：鉴权拦截时序

```
前端请求 (Header: Authorization: Bearer xxx)
    │
    ▼
WebMvcConfig 拦截器链
    │
    ▼
AuthInterceptor.preHandle()
    │
    ├─ 1. URI 在排除列表? (/auth/**, /swagger-ui/**, /error)
    │      是 → 放行
    │
    ├─ 2. 解析 Authorization Header
    │      缺失 → 401 "未登录或token缺失"
    │
    ├─ 3. JwtUtil.parse(token) → 取 userId, role
    │      异常 → 401 "token无效或已过期"
    │
    ├─ 4. request.setAttribute("userId", userId)
    │      request.setAttribute("role", role)
    │
    ├─ 5. 查 ROLE_ROUTES.get(role) 白名单
    │      role 未知 → 403 "未知角色"
    │
    ├─ 6. 遍历白名单 matchRoute(pattern, uri)
    │      匹配 → 放行
    │
    └─ 7. 无匹配 → 403 "您没有权限访问该接口"

    Controller 内部再做数据级校验：
    ├─ cancel/restore: visitorId == 当前用户?
    └─ approve(host角色): hostId == 当前用户?
```

---

## 快速上手 Checklist

- [ ] 克隆代码，确认 `backend/` + `demofront/` + `forSql/` 三个目录完整
- [ ] 执行 **2 个** SQL 脚本初始化数据库（init.sql + insert.sql）
- [ ] 设置环境变量或修改 `application-dev.yml`（DB_PASSWORD 等）
- [ ] `mvn spring-boot:run` 启动后端，访问 Swagger 确认
- [ ] 微信开发者工具导入 `demofront`，勾选不校验合法域名
- [ ] 用 `admin/admin123` 登录管理员端，熟悉 9 个管理页面
- [ ] 用 `zhangsan/123456` 登录被访人端，走一遍审批流程
- [ ] 用 `visitor1/123456` 登录访客端，提交预约 → 查看二维码
- [ ] 用 `guard/123456` 登录门岗端，扫码核验 → 确认放行
- [ ] 观察审批通过后 `app_greeting` 表是否生成 AI 话术
- [ ] 管理员进入「通知管理→我的通知」标签页，查看是否有风险预警通知
- [ ] 访客提交预约后查看 `app_risk_assessment` 表是否有风险评估记录
- [ ] 管理员审批页展开待审核预约，查看 AI 风险预警标签和理由
- [ ] 拒绝一条预约后再次提交相似预约，观察风险等级是否升高
- [ ] 进入通知中心查看未读通知，点击即标记已读
- [ ] 阅读 `AppointmentStatus.java` 理解状态常量
- [ ] 阅读 `AppointmentService.approve()` 理解事务边界
- [ ] 阅读 `DeepSeekClient.java` 理解推理模型适配（max_tokens 调优 + 零示例 Prompt + lastIndexOf JSON 解析）
- [ ] 阅读 `RiskVectorStore.java` 理解 RAG 向量检索 v2.0（加权融合 ∈[0,1] + 动态阈值下限 0.10 + LRU 分词缓存）
- [ ] 阅读 `GreetingCacheService.java` 理解话术缓存（Redis + 内存降级）
- [ ] 阅读 `AppointmentService.assessRiskIfNeeded()` 理解风险预警流程 + `checkHardRules()` 硬规则
- [ ] 阅读 `AsyncConfig.java` 理解 AI 异步线程池
- [ ] 阅读 `ReminderScheduler.java` 理解定时提醒 + 风险数据清理 + 缓存清除
- [ ] 阅读 `AppointmentService.checkTimeConflict()` 理解时间冲突校验
- [ ] 阅读 `records.js` 中 `saveViaCanvas()` / `saveViaFile()` 理解 QR 保存双路径
- [ ] **精读 [`重要功能.md`](重要功能.md) 理解两个 AI 功能的完整架构与答辩亮点**

---

> **文档版本**：v2.3 · 已同步推理模型适配（max_tokens 调优 + 零示例 Prompt + lastIndexOf JSON 解析）+ 风险判定三级重构（ruleBased 永不判 high + 硬规则同记录匹配 + AI→high 安全降级）+ QR Canvas 双路径 + 再次预约功能删除。
