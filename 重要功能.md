# AI 重要功能详解

> 企业访客预约系统两个 AI 核心功能的技术文档，含架构图、调用链路、降级策略、答辩要点。

---

## 目录

- [一、AI 接待话术智能生成](#一ai-接待话术智能生成)
- [二、AI 异常访客风险预警](#二ai-异常访客风险预警)
- [三、配置说明](#三配置说明)
- [四、测试验证](#四测试验证)

---

## 一、AI 接待话术智能生成

### 功能定位

审批通过后自动生成个性化迎接方案（欢迎语 + 座位建议 + 注意事项），通过通知推送提醒被访人做好准备。

### 技术架构

```
审批通过 → @Async("aiExecutor")
  └─ AppointmentService.generateGreetingIfNeeded()
       └─ DeepSeekClient.generateGreeting()
            ├─ ① Redis/内存缓存命中(12h) → source="cache"
            ├─ ② 零示例 Prompt 调推理模型(30s超时, max_tokens=4096) → source="ai"
            └─ ③ 场景模板mock(按事由匹配) → source="mock"
                 └─ 存 app_greeting 表
```

### 三级降级

| 级别 | 条件 | source | 耗时 | 消耗 |
|------|------|--------|------|------|
| 一级 | 缓存命中 | `cache` | ~2ms | 零 token |
| 二级 | AI 正常 | `ai` | ~5-15s | ~1000-4000 token |
| 三级 | AI 失败/超时 | `mock` | 0ms | 零 token |

### 推理模型适配（关键）

**模型**：`deepseek-v4-flash-free` 是推理模型，与普通模型行为不同：

| 特性 | 普通模型 | 推理模型 |
|------|---------|---------|
| 输出顺序 | 直接回答 | 先思考→再回答 |
| Few-Shot Prompt | 有效 | **被当成分析题**，大量 token 消耗于'审题' |
| 最小 token | ~50 | ~2000（思维链） |
| JSON 解析 | `indexOf('{')` | **`lastIndexOf('{"greeting"')`**（推理文本在前） |
| max_tokens | 300 | **4096**（推理阶段需大量 token） |

**适配要点**：
- Prompt 去掉所有 Few-Shot 示例，直接指令
- JSON 解析取最后一个 `{"greeting"` 位置，跳过前置推理文本
- 超时从 15s 提到 30s，max_tokens 从 300 提到 4096
- content 为空时检查 reasoning_content 并告警

### 缓存机制

- **GreetingCacheService**：SHA-256(姓名+单位+事由+被访人) 为键
- **Redis 主存储**，12h TTL
- **Redis 不可达自动降级** ConcurrentHashMap 内存缓存
- **Regenerate 接口**：`evict(cacheKey)` → 直调 AI

### 场景模板库（mock 兜底）

按来访事由包含的关键词匹配：**签约 → 合作 → 技术 → default**

### 核心文件

| 文件 | 作用 |
|------|------|
| `ai/DeepSeekClient.java` | generateGreeting() + callAiGreeting() + scenarioMock() |
| `ai/GreetingCacheService.java` | Redis + 内存双重缓存 |
| `controller/GreetingController.java` | 查看/生成/重新生成话术 API |
| `service/AppointmentService.java` | generateGreetingIfNeeded() @Async 触发 |
| `controller/GuardController.java` | 核验时 sendGreetingNotification() 含 seatSuggestion |

### 相关 API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/ai/greeting/{appointmentId}` | 生成/获取话术 |
| POST | `/ai/greeting/{appointmentId}/regenerate` | 清除缓存重新生成 |
| GET | `/ai/appointment/{appointmentId}/greeting` | 查看已生成话术 |

---

## 二、AI 异常访客风险预警

### 功能定位

新预约提交时自动检索历史被拒记录 → 大模型评估风险等级 → 高风险通知管理员重点审核。标准 RAG 架构。

### 技术架构

```
新预约提交 → @Async("aiExecutor")
  └─ AppointmentService.assessRiskIfNeeded()
       ├─ 1. RAG 检索：RiskVectorStore.search() → Top-5 相似被拒记录
       │      TF-IDF 向量化 → 余弦相似度 → 加权融合(∈[0,1]) → 动态阈值过滤
       │      + 手机号/姓名精确匹配加权 + 公司模糊匹配加权
       │      计算平均相似度 avgSim 传入下游
       │
       ├─ 2. 硬规则优先：AppointmentService.checkHardRules()
       │      同记录内手机号+姓名/单位双匹配 → high (零token)
       │
       ├─ 3. 未命中硬规则 → DeepSeekClient.assessRisk(appt, cases, avgSim)
       │      max_tokens=2048, 简化Prompt, 30s超时
       │       ├─ ① 24h缓存命中(键含访客标识) → source="cache"
       │       ├─ ② 调大模型 → source="ai"
       │       └─ ③ 规则兜底(永不判high) → source="rule_fallback"
       │
       └─ 4. 安全锁：非 hard_rule 源的 high → 强制降 medium
            └─ 存 app_risk_assessment → high时通知管理员，被访人不可审批通过
```

### 自研 RAG 向量库 RiskVectorStore v2.0

**全 JDK 原生实现，零外部依赖。**

#### 向量化流程

```
被拒预约 → 文本拼接(name+company+purpose+PHONE:+NAME:)
  → 字符二元组分词(缓存) + 精确token
  → TF-IDF = TF × log(1+N/(1+df))
  → L2 归一化
```

#### v2.0 优化

| 特性 | 实现 | 收益 |
|------|------|------|
| 增量 IDF | `add()` 仅更新新文档 token | O(新增token) vs O(N²) |
| 读写锁 | `ReentrantReadWriteLock` | 搜索并发读不互斥 |
| 分词缓存 | `LinkedHashMap` LRU 5000 上限 | 重复文本不切分，超限淘汰最旧 |
| 动态阈值 | `max(0.10, 0.15 - log10(N)×0.01)` | 过滤 2-gram 通用词假阳性 |
| 相似度归一化 | `sim = cosine + bonus×(1-cosine)` | 加权融合保证 ∈[0,1]，避免 >1 |
| TopK 堆排序 | `PriorityQueue` 定长 | 避免全量排序 |
| 提前剪枝 | `hasIntersectingTokens()` | 跳过无交集文档 |
| 手机号/姓名索引 | `findByPhone()` / `findByName()` | O(1) 快速检索 |
| 可配置权重 | `setWeights(phone, name, company)` | 运行时调优 |

### 多级降级

| 级别 | 条件 | source |
|------|------|--------|
| 一级 | 24h 缓存命中 | `cache` |
| 二级 | 硬规则命中(同手机号/姓名) | `hard_rule` |
| 三级 | AI 正常 | `ai` |
| 四级 | AI 失败/429/超时 | `rule_fallback` |

> **缓存键设计**：`risk:姓名|手机|单位|事由|匹配数`。包含访客标识，避免不同访客同单位同事误命中他人缓存。
> **缓存清除时机**：拒绝预约 / 重新评估 / 重建向量库 / 定时清理时调用 `clearAllRiskCache()`，保证后续评估基于最新知识库。

### 三级风险判定标准

#### 🔴 高风险 high（仅硬规则）
- 同一条被拒记录内：手机号+姓名均匹配 → high(95)
- 同一条被拒记录内：手机号+单位均匹配 → high(85)
- **业务处理**：锁定 pending，通知管理员，**被访人不可审批通过**

#### 🟠 中风险 medium
- RAG 检索到相似记录但无同记录内手机号+姓名/单位双匹配
- AI 判断或规则兜底（40-65分）
- **业务处理**：被访人可见橙色风险提示，可自主审批

#### 🟢 低风险 low
- RAG 无检索结果或相似度极低
- **业务处理**：无风险提示，正常流转

> **安全锁**：`assessRiskIfNeeded` 中硬编码保护——非 `hard_rule` 源返回 high 时强制降为 medium。确保仅硬规则可判 high，AI/缓存/兜底不可越权。

### 硬规则优先检测（跳过 AI，零 token）

在 `AppointmentService` 中，RAG 检索后**同一条记录内**检查手机号/姓名/单位精确匹配（避免跨记录伪匹配）：

- 同一条被拒记录：手机号+姓名均匹配 → high (95) 
- 同一条被拒记录：手机号+单位均匹配 → high (85)
- 不满足以上 → 不触发硬规则，交给 AI/ruleBased

### 规则兜底动态评分

`ruleBased(matchCount, avgSim)` **永不判 high**，最高 medium：

| 匹配数 | 基础分 | 相似度加成 | 上限 | 等级 |
|--------|--------|-----------|------|------|
| ≥2 | 40 | +avgSim×25 | 65 | medium |
| 1 | 30 | +avgSim×20 | 55 | medium |
| 0 | 12 | — | — | low |

> 真正的 high 仅来自硬规则（明确证据）或 AI 语义判断，规则兜底不自动升级。

### 定时维护

`ReminderScheduler`：
- 每 5 分钟：预约开始前 30 分钟提醒访客
- 每天 3:00：删除 90 天前 `app_risk_assessment` + 重建向量库

### 核心文件

| 文件 | 作用 |
|------|------|
| `ai/RiskVectorStore.java` | 自研 TF-IDF 向量库 v2.0 |
| `ai/DeepSeekClient.java` | assessRisk() + 硬规则 + 容错 |
| `service/AppointmentService.java` | assessRiskIfNeeded() @Async |
| `controller/RiskController.java` | 风险查看/重评/权重/索引查询 |
| `entity/RiskAssessment.java` | app_risk_assessment 表实体 |
| `task/ReminderScheduler.java` | 定时清理+预约提醒 |

### 相关 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/admin/risk/appointment/{id}` | 查看风险评估 |
| GET | `/admin/risk/high` | 高风险列表 |
| POST | `/admin/risk/appointment/{id}/reassess` | 清除缓存重新评估 |
| PUT | `/admin/risk/store/weights` | 调整相似度权重 |
| GET | `/admin/risk/store/status` | 向量库状态 |
| POST | `/admin/risk/store/rebuild` | 手动重建向量库 |
| GET | `/admin/risk/store/lookup/phone?phone=` | 按手机号查 |
| GET | `/admin/risk/store/lookup/name?name=` | 按姓名查 |

### 前端展示

- **管理员审批页**：展开待审核预约 → 风险标签(红/橙/绿) + 风险分 + 理由 + **重新评估按钮**
- **管理员通知页**：「我的通知」标签 → ⚠️ 风险预警 → 点击跳审核页

---

## 三、配置说明

```yaml
# application.yml
deepseek:
  api-key: ${DEEPSEEK_API_KEY:sk-xxx}
  api-url: ${DEEPSEEK_API_URL:https://opencode.ai/zen/v1/chat/completions}
  model: ${DEEPSEEK_MODEL:deepseek-v4-flash-free}    # 推理模型
  greeting:
    timeout-seconds: 30          # 推理模型需要更长超时（30s）

# Redis 可选，不可达自动降级内存缓存
spring.data.redis:
  host: ${REDIS_HOST:localhost}
  port: ${REDIS_PORT:6379}

# 话术 max_tokens=4096, 风险 max_tokens=2048（代码内硬编码）
# MyBatis SQL 调试日志已关闭（排查问题时注释掉避免淹没业务日志）
```

---

## 四、测试验证

### 话术生成

1. `visitor1 / 123456` 登录 → 提交预约(被访人张三)
2. `zhangsan / 123456` 登录 → 审批通过
3. **等 5-15 秒**（推理模型较慢）→ 查 `app_greeting` 表
4. 日志：`[话术] AI生成成功` 或 `[话术] Mock模板生成`(API不可用时)

### 风险预警 — 高风险测试

1. `visitor2 / 123456`(陈访客/13900000002) 登录 → 提交预约(单位ZZ集团/事由技术交流)
2. **等 3-8 秒** → 日志：硬规则命中(同手机号+同姓名来自同一条被拒记录) → high(95)
3. `admin / admin123` 登录 → 审批页展开预约 → 🔴 高风险标签 + `本地规则`
4. `zhangsan / 123456` 尝试审批 → 弹窗「该预约被AI评估为高风险，需管理员审核」

### 风险预警 — 中风险测试

1. 新注册访客（手机号 13900000888，姓名"赵新"，**不要复用被拒记录的手机号/姓名**）
2. 提交预约(单位ZZ集团/事由技术交流)
3. 等 3-8 秒 → 硬规则未命中(手机号不匹配) → AI/ruleBased → `level=medium`
4. `zhangsan / 123456` 登录 → 🟠 中风险标签，可正常审批

### 风险预警 — 低风险测试

1. `visitor3 / 123456`(吴访客/13900000003) 登录 → 提交预约(单位HW技术/事由产品演示)
2. 等 3-8 秒 → `[风险] 评估完成 level=low`
3. 🟢 低风险标签，无额外通知

> **测试要点**：三个风险等级对应三种访客身份——
> - **高风险**：手机号+姓名都与被拒记录相同（如 visitor2/陈访客/13900000002）
> - **中风险**：手机号/姓名都不同，但单位/事由与被拒记录相似
> - **低风险**：手机号/姓名/单位/事由都与被拒记录无关

### 降级测试

| 场景 | 操作 | 话术source | 风险source |
|------|------|-----------|-----------|
| AI 不可用 | 清空 DEEPSEEK_API_KEY | `mock` | `rule_fallback` |
| 重复预约 | 相同信息再提交 | `cache` | `cache` |

### 验证 SQL

```sql
-- 话术
SELECT * FROM app_greeting ORDER BY id DESC LIMIT 3;
-- 风险评估(三种等级)
SELECT appointment_id, risk_level, risk_score, LEFT(reason,60) FROM app_risk_assessment ORDER BY id DESC LIMIT 5;
-- 风险通知
SELECT * FROM sys_user_notification WHERE type='risk_warning' ORDER BY id DESC;
```

---

> **版本**：v2.3 · 推理模型全适配（零示例 Prompt + max_tokens 调优 + lastIndexOf JSON 解析）+ 风险安全锁 + 话术 regenerate 缓存修复。
